/**
 * Stub type declarations for @anthropic-ai/claude-agent-sdk (not published on npm).
 * Only PermissionMode type is imported by the source.
 */

export type PermissionMode = 'default' | 'plan' | 'auto' | 'bypassPermissions' | string;
